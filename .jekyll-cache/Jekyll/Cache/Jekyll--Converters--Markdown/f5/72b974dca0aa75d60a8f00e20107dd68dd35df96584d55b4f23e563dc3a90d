I"4<h2 id="test-ecriture-code">Test ecriture code</h2>
<p>Salut à tous je fais un test</p>

<div class="language-plaintext highlighter-rouge"><div class="highlight"><pre class="highlight"><code>
from statistics import mean
def is_numeric(txt):
    try:
        float(txt)
        return True
    except ValueError:
        return False
        
while(True):
    try:
        line = input().split()
        print(mean([float(x) for x in line if is_numeric(x)]), " ".join([x for x in line if x.isalpha()]))
    except EOFError:
        break
</code></pre></div></div>
:ET